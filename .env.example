# Frontend (Vite) public variables – safe to commit as placeholders
# These are embedded in the client bundle. Only use PUBLIC/ANON keys here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# NOTE: Do NOT put sensitive keys (e.g., service role, Gemini API) here.
# Configure them as Supabase Edge Function secrets instead.
# Example secret name expected by current functions: VITE_GEMINI_API_KEY
# Set it using the Supabase CLI or Dashboard (see README or project instructions).
